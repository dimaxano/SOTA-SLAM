namespace sotaslam {
    class system {
        system();
        ~system();

        /** startup/shutdown
         *  initalizing and storing for high level access: fronted, backend, map info
         *  image feeding
         *  reset
         * 
         */

    };
}